server:
  port: 8080

spring:
  # 開発で必要があればここに追加設定

flyway:
  clean-disabled: false       # 開発ではクリーン許可

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui
    operationsSorter: alpha
    tagsSorter: alpha
    tryItOutEnabled: true

datasource:
  # 値は全て環境変数から注入（デフォルトなし）
  url:  jdbc:postgresql://localhost:5432/billing?sslmode=disable
  username: syukatsusysss
  password: letsstartdb7392

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: TRACE
    org.hibernate.SQL: DEBUG
  # org.hibernate.type.descriptor.sql: TRACE  # バインド値まで見る場合のみ一時的に
  pattern:
    level: "%5p [%X{rid}]"
